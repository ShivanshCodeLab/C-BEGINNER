#include <stdio.h>

    int main(){

        int p = 100;
        float r = 5;
        int t = 5;


        // value of SI can be decimal so we used flaot 
        float SI = (p * r * t)/100;

        printf("Simple Interest is %f", SI);

    return 0;
}