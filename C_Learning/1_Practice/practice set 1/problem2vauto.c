#include <stdio.h>

    int main(){
        int r = 6;
        int h = 5;
       printf("The volume of a cylinder with radius %d and height %d is %.2f",
           r, h, 3.14 * r * r * h);

    return 0;
}