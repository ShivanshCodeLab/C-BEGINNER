#include <stdio.h>

    int main(){
        int radius = 5;
        int height = 5;
        
        // To get area of circle
        printf("The area of circle is %d is %f",radius, 3.14 * radius * radius);


        // To get volume of cylinder
        printf("\nThe volume of cylinder is %d is %d  is %f", radius, height, 3.14 * radius * radius *height);

    return 0;
}