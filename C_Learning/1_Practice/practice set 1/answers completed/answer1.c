#include <stdio.h>

    int main(){

        // Length and breadth of rectangle
        int length = 5;
        int breadth = 5;

        // To print the area
        printf("The area of rectangle is %d", length * breadth);
    
    return 0;
}