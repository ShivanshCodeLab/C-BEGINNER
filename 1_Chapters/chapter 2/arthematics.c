#include <stdio.h>

    int main(){
        int a = 9;
        int b = 5;
        int c = a + b;
        

        printf("The value of a is %d and value of b is %d and sum of values is %d\n", a, b, c);
    
    //modulus operator is used to get the remainder
    printf("The reminder when a is divided by b is %d", a%b);
    
        return 0;
}